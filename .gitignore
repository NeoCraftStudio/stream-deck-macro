# Secrets
obs_secrets.json

# Python
__pycache__/
*.pyc
.venv/
venv/
build/
dist/
dist_installer/
*.spec

# Personal runtime config — this is data, not source. The app itself
# stores the real one in %APPDATA%\NeoCraft Macro Desk\config.json when
# installed; this file only exists locally during dev-mode runs.
config.json

# Arduino / PlatformIO
.pio/
*.hex
*.elf

# OS
Thumbs.db
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
